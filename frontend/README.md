# Frontend

Angular console for the log parser API.

```bash
npm ci
npm start
```

Open `http://127.0.0.1:4200`. The UI proxies `/api` to the FastAPI process on port 8000.

```bash
npm test
npm run lint
npx playwright test
```
